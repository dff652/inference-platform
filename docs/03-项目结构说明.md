# 项目结构说明

## 最后更新：2026-03-17

## 目录结构

```
inference-platform/
├── backend/
│   ├── app/
│   │   ├── api/                        # FastAPI 路由层
│   │   │   ├── tasks.py                # 推理任务：CRUD + 提交/取消/重试 + 结果/日志/图表数据
│   │   │   ├── models.py              # 模型中心：CRUD + 激活/归档 + GPU 服务状态
│   │   │   ├── configs.py             # 配置模板 + 算法列表 + ChatTS Prompt 模板
│   │   │   ├── predict.py             # 同步预测 API（快速验证用）
│   │   │   ├── uploads.py             # 文件上传
│   │   │   └── data_sources.py        # 数据源：CRUD + 连通性测试
│   │   ├── algorithms/                 # 算法层（直接集成，不走 subprocess）
│   │   │   ├── dispatcher.py          # 统一调度器：run() CPU / run_vllm() GPU
│   │   │   ├── iforest.py             # Isolation Forest 检测
│   │   │   ├── ensemble.py            # Ensemble 投票法检测
│   │   │   ├── wavelet.py             # 小波检测
│   │   │   ├── adtk_hbos.py           # ADTK-HBOS 统计检测
│   │   │   ├── lb_eval.py             # 在线段评分：KS-test + 边界差异分析（0-100分）
│   │   │   ├── eval_metrics.py        # 离线评估：Precision/Recall/F1/MCC/MAR/FAR + batch
│   │   │   ├── helpers.py             # 辅助函数：mask/split/merge + 小波高级处理（聚类/精炼/扩展）
│   │   │   ├── preprocessing.py       # 预处理 + 数据质量：降采样/STL/噪声检测/variance_filter
│   │   │   └── configs/
│   │   │       └── chatts_prompts.json # ChatTS Prompt 模板（8个）
│   │   ├── adapters/                   # 外部服务适配器
│   │   │   ├── vllm_backend.py        # GPU 推理 HTTP 客户端（ChatTS/Qwen）
│   │   │   └── executor_adapter.py    # CLI 适配器（subprocess 回退路径）
│   │   ├── models/                     # SQLAlchemy ORM 模型
│   │   │   ├── inference_task.py      # 推理任务（含 TaskStatus 枚举）
│   │   │   ├── model_entity.py        # 模型实体
│   │   │   ├── config_template.py     # 推理配置模板
│   │   │   ├── data_source.py         # 数据源
│   │   │   └── result_index.py        # 推理结果索引
│   │   ├── schemas/                    # Pydantic 请求/响应模型
│   │   ├── services/                   # 业务逻辑层
│   │   │   ├── state_machine.py       # 任务状态机（转换规则 + 校验）
│   │   │   ├── task_service.py        # 任务服务（CRUD + 状态流转）
│   │   │   └── model_service.py       # 模型服务
│   │   ├── core/                       # 基础设施
│   │   │   ├── config.py              # Pydantic Settings 配置
│   │   │   ├── database.py            # Async SQLAlchemy 引擎
│   │   │   ├── celery_app.py          # Celery 应用 + 三路推理调度
│   │   │   └── perf_logger.py         # 性能监控：PerfMetrics + PerfLogger + Timer + GPU/系统指标
│   │   └── main.py                     # FastAPI 入口
│   ├── data/                           # 运行时数据
│   │   ├── inference/                 # 推理输出文件（按 task_id 分目录）
│   │   ├── uploads/                   # 上传文件存储
│   │   └── inference_platform.db      # SQLite 数据库
│   └── requirements.txt
├── frontend/                           # Vue 3 前端
│   ├── src/
│   │   ├── api/index.js               # Axios API 客户端
│   │   ├── views/
│   │   │   ├── TaskList.vue           # 任务看板（统计卡片 + 筛选表格 + 5s 轮询）
│   │   │   ├── TaskCreate.vue         # 创建推理任务（算法选择 + Prompt 模板 + 文件上传）
│   │   │   ├── TaskDetail.vue         # 任务详情（Info/Results/Logs + D3 图表 + 段评分）
│   │   │   ├── ModelCenter.vue        # 模型中心（GPU 状态卡片 + 模型注册表）
│   │   │   ├── DataSources.vue        # [待建] 数据池（数据源 CRUD + 连通性测试）
│   │   │   ├── Annotator.vue          # [待建] 数据标注（LabelerD3 交互标注）
│   │   │   └── Evaluation.vue         # [待建] 模型评估（标注对比 + 多指标展示）
│   │   ├── components/
│   │   │   └── TimeSeriesChart.vue    # D3.js 时序可视化（主图 + 上下文图 + 异常高亮）
│   │   ├── router/index.js
│   │   └── utils/constants.js
│   └── package.json                    # Vue 3 + Element Plus + D3.js
├── services/                           # GPU 推理服务（独立进程）
│   ├── chatts_serve.py                # ChatTS-8B serve（transformers, GPU 0, :8001）
│   ├── qwen_serve.py                  # Qwen3-VL-8B serve（transformers, 已弃用，改用 vLLM）
│   ├── run_qwen_vllm.sh              # Qwen vLLM 0.11.0 启动脚本（systemd 调用, GPU 1, :8002）
│   └── start_vllm.sh                 # 旧启动脚本（支持 chatts/qwen/all）
├── scripts/
│   └── platform.sh                    # 服务管理：start/stop/restart/status/logs/enable/disable
├── docker/
│   └── Dockerfile
├── docs/                               # 项目文档
├── docker-compose.yml
└── .gitignore
```

## API 接口清单

### 推理任务 /api/v1/inference/tasks

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | / | 创建任务 |
| GET | / | 任务列表（支持 status/submitter/algorithm 筛选） |
| GET | /stats | 各状态任务统计 |
| GET | /{id} | 任务详情 |
| PUT | /{id} | 更新任务 |
| POST | /{id}/submit | 提交执行 |
| POST | /{id}/cancel | 取消任务 |
| POST | /{id}/retry | 重试任务 |
| GET | /{id}/results | 推理结果详情（含 segments） |
| GET | /{id}/logs | 执行日志 |
| GET | /{id}/results/{rid}/chart-data | D3 图表数据 |

### 同步预测 /api/v1/predict

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | / | 同步预测（直接返回结果，不入 Celery） |

### 模型中心 /api/v1/models

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | / | 注册模型 |
| GET | / | 模型列表（支持 family/status 筛选） |
| GET | /{id} | 模型详情 |
| PUT | /{id} | 更新模型 |
| POST | /{id}/activate | 激活模型 |
| POST | /{id}/archive | 归档模型 |
| GET | /vllm/status | GPU 服务健康状态 |

### 配置管理 /api/v1/inference/configs

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /algorithms | 支持的算法列表（含 resource 标记） |
| GET | /chatts-prompts | ChatTS Prompt 模板列表 |
| POST | / | 创建配置模板 |
| GET | / | 配置列表 |
| PUT | /{id} | 更新配置 |

### 数据源 /api/v1/data-sources

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | / | 创建数据源 |
| GET | / | 数据源列表 |
| GET | /{id} | 数据源详情 |
| PUT | /{id} | 更新数据源 |
| POST | /{id}/test | 连通性测试 |

### 文件上传 /api/v1/uploads

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | / | 上传 CSV/TXT/XLSX 文件 |

## 前端页面结构

### 侧边栏导航

```
推理平台
├── 任务中心
│   ├── 任务看板            /tasks              TaskList.vue        ✅ 已实现
│   ├── 创建推理任务        /tasks/create       TaskCreate.vue      ✅ 已实现
│   └── (任务详情)          /tasks/:id          TaskDetail.vue      ✅ 已实现（不在侧边栏）
├── 数据管理
│   ├── 数据池              /data-sources       Placeholder.vue     🚧 占位
│   └── 数据标注            /annotate           Placeholder.vue     🚧 占位
├── 模型管理
│   ├── 模型中心            /models             ModelCenter.vue     ✅ 已实现
│   ├── 模型评估            /evaluation         Placeholder.vue     🚧 占位
│   └── 模型微调            /training           Placeholder.vue     🚧 占位
├── 系统管理
│   ├── 用户管理            /users              Placeholder.vue     🚧 占位
│   └── 系统设置            /settings           Placeholder.vue     🚧 占位
```

### 各页面功能设计

```
/tasks — 任务看板
├── 统计卡片（总数/运行中/成功/失败）
├── 筛选表格（状态/算法/提交者 + 5s 轮询）
└── 操作按钮（提交/取消/重试/查看详情）

/tasks/create — 创建推理任务
├── 算法选择（CPU/GPU 分组，resource 标签）
├── 文件上传（CSV/TXT/XLSX）
├── GPU 选项（ChatTS: Prompt 模板选择 / Max Tokens）
├── [待做] 数据质量预检（上传后自动检测噪声/死值/阶跃 → 算法推荐）
└── 提交执行

/tasks/:id — 任务详情
├── Tab: Info（基本信息 + 参数 + 状态历史）
├── Tab: Results（D3 时序图 + 段评分表 + Score/KS/Boundary/Label）
├── Tab: Logs（执行日志文本）
├── [待做] Tab: Performance（时间分解瀑布图 + GPU/系统指标）
└── [待做] "查看标注" 按钮 → 跳转 /annotate?task_id=xxx

/data-sources — 数据池 [待开发，后端 API 已就绪]
├── 数据源列表（CRUD + 状态标签）
├── 连通性测试（IoTDB / 文件系统）
├── 文件浏览器（已上传文件列表 + 预览）
└── 数据导入（从数据源拉取 → 生成本地 CSV）

/annotate — 数据标注 [待开发]
├── 文件选择（上传 CSV / 从数据池选择 / 从推理结果加载）
├── D3.js 交互标注器（LabelerD3 完整能力）
│   ├── 主图 + 缩略图双视图
│   ├── 点击标注 / 拖拽选区 / 键盘导航
│   ├── 双 Y 轴 / 多系列切换
│   └── 异常类型选择（全局/局部/趋势/周期 等）
├── 标注导出（JSON 格式，兼容 eval_metrics）
└── 标注历史管理

/models — 模型中心
├── GPU 服务状态卡片（ChatTS/Qwen Online/Offline + 端点信息）
├── 模型注册表（CRUD + 激活/归档）
└── 模型详情（参数/路径/显存/状态）

/evaluation — 模型评估 [待开发]
├── 选择推理任务结果（单选或多选对比）
├── 上传/选择标注数据（ground truth，来自标注模块）
├── 运行评估 → calculate_combined_metrics()
├── 指标卡片（Precision / Recall / F1 / MAR / FAR / Harmonic Acc）
├── 多算法对比表 + 雷达图
└── 评估历史记录

/training — 模型微调 [预留]
├── 训练数据管理（标注数据 → 训练格式转换）
├── 微调任务创建（LLaMA-Factory 参数配置）
├── 训练监控（loss 曲线 / epoch 进度）
└── 模型版本管理（训练产物 → 注册到模型中心）

/users — 用户管理 [预留]
├── 用户列表（CRUD）
├── 角色/权限配置
└── API Key 管理

/settings — 系统设置 [预留]
├── 算法参数模板管理
├── 平台全局配置（Redis/DB/GPU 端点）
└── 日志级别 / 调试开关
```

### 全局布局

```
┌─────────────────────────────────────────────────────────────┐
│ el-container (min-height: 100vh)                            │
│ ┌──────────┬──────────────────────────────────────────────┐ │
│ │ el-aside │ el-container                                 │ │
│ │ 210px    │ ┌──────────────────────────────────────────┐ │ │
│ │ #304156  │ │ el-header                                │ │ │
│ │          │ │ border-bottom: 1px solid #e6e6e6         │ │ │
│ │ 推理平台  │ │ el-breadcrumb（首页 / 当前页标题）        │ │ │
│ │          │ ├──────────────────────────────────────────┤ │ │
│ │ el-menu  │ │ el-main                                  │ │ │
│ │ (router) │ │ background: #f0f2f5; padding: 20px      │ │ │
│ │          │ │ ┌──────────────────────────────────────┐ │ │ │
│ │ 4组      │ │ │ <router-view />                      │ │ │ │
│ │ sub-menu │ │ │ 各页面内容区域                        │ │ │ │
│ │          │ │ └──────────────────────────────────────┘ │ │ │
│ └──────────┴──────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

- **侧边栏菜单**: `el-menu` + `router` 模式，`default-active` 绑定 `$route.path`
- **面包屑**: `el-breadcrumb`，显示 `$route.meta.title` 作为当前页中文标题
- **子菜单样式**: `.el-sub-menu .el-menu-item { padding-left: 50px !important }`

### 颜色与状态映射

#### 任务状态 → el-tag type（constants.js）

| 状态 | tag type | 颜色效果 | 说明 |
|------|----------|----------|------|
| draft | info | 灰色 | 草稿 |
| pending | warning | 橙色 | 等待中 |
| queued | warning | 橙色 | 排队中 |
| running | '' (primary) | 蓝色 | 运行中 |
| completed | success | 绿色 | 完成 |
| failed | danger | 红色 | 失败 |
| cancelled | info | 灰色 | 已取消 |
| timeout | danger | 红色 | 超时 |

#### 模型状态 → el-tag type（ModelCenter.vue）

| 状态 | tag type | 颜色效果 |
|------|----------|----------|
| active | success | 绿色 |
| archived | info | 灰色 |
| disabled | danger | 红色 |

#### 段评分 → el-tag type（TaskDetail.vue）

| 分数范围 | tag type | 含义 |
|----------|----------|------|
| score ≥ 50 | danger | 高可疑异常 |
| 20 ≤ score < 50 | warning | 中等可疑 |
| score < 20 | info | 低可疑 |

#### GPU 服务状态

| 状态 | tag type + effect |
|------|-------------------|
| healthy=true | success + dark → "Online" |
| healthy=false | danger + dark → "Offline" |

#### 品牌/主题色

| 用途 | 色值 |
|------|------|
| 侧边栏背景 | #304156 |
| 菜单文字 | #bfcbd9 |
| 菜单选中 | #409eff (Element Plus primary) |
| 主内容区背景 | #f0f2f5 |
| 辅助文字 | #909399 |
| D3 时序线 | #409EFF |
| D3 异常区域/点 | #F8766D |

### 组件使用规范

#### 已使用的 Element Plus 组件清单

**布局**: el-container, el-aside, el-header, el-main, el-row, el-col, el-card, el-divider, el-space

**导航**: el-menu, el-sub-menu, el-menu-item, el-breadcrumb, el-breadcrumb-item, el-tabs, el-tab-pane

**数据展示**: el-table, el-table-column, el-tag, el-descriptions, el-descriptions-item, el-empty, el-link

**表单**: el-form, el-form-item, el-input, el-input-number, el-select, el-option, el-switch, el-radio-group, el-radio-button, el-upload

**反馈**: el-button, el-dialog, el-alert, el-message, el-message-box, el-pagination

**图标**: List, Plus, Box, Folder, EditPen, TrophyBase, Cpu, Setting, User, UploadFilled

### 表格列规格

#### TaskList.vue 任务看板表格

| 列名 | prop | 宽度 | 格式 |
|------|------|------|------|
| ID | id | 60px | 数字 |
| Task Name | task_name | min-width: 180px | el-link 可点击跳转详情 |
| Algorithm | algorithm_name | 130px | 文本 |
| Status | status | 110px | el-tag（statusColors 映射） |
| Submitter | submitter | 100px | 文本 |
| Created | created_at | 170px | toLocaleString() |
| Duration | started_at/completed_at | 100px | 计算秒数 / "Running..." / "-" |
| Actions | - | 200px, fixed="right" | Submit/Cancel/Retry/Detail 按钮组 |

#### TaskDetail.vue 推理结果表格

| 列名 | prop | 宽度 | 格式 |
|------|------|------|------|
| Point | point_name | 150px | 文本 |
| Method | method | 130px | 文本 |
| Score Avg | score_avg | 110px | toFixed(2) |
| Score Max | score_max | 110px | toFixed(2) |
| Segments | segment_count | 100px | 数字 |
| Visualization | - | 120px | "View Chart" 按钮（懒加载） |

#### TaskDetail.vue 异常段表格

| 列名 | prop | 宽度 | 格式 |
|------|------|------|------|
| Start | start | 80px | 数字 |
| End | end | 80px | 数字 |
| Length | length | 70px | 数字 |
| Score | score | 90px | el-tag（分数颜色映射），toFixed(1) |
| KS p-value | raw_p | 100px | toFixed(4) |
| Boundary L/R | left/right | 130px | toFixed(2) / toFixed(2) |
| Label | label | 120px | show-overflow-tooltip |
| Detail | detail | min-width: 200px | show-overflow-tooltip |

#### ModelCenter.vue 模型注册表格

| 列名 | prop | 宽度 | 格式 |
|------|------|------|------|
| ID | id | 60px | 数字 |
| Name | name | min-width: 160px | 文本 |
| Family | family | 130px | 文本 |
| Runtime | runtime_type | 140px | 文本 |
| Version | version | 80px | 文本 |
| Status | status | 90px | el-tag（模型状态映射） |
| Tags | tags | min-width: 180px | el-tag 数组 |
| Artifact URI | artifact_uri | min-width: 200px | show-overflow-tooltip |
| Actions | - | 220px, fixed="right" | Edit/Activate/Archive 按钮组 |

### 表单字段规格

#### TaskCreate.vue 创建推理任务表单

| 字段 | 组件 | 必填 | 验证/约束 |
|------|------|------|-----------|
| Task Name | el-input | ✅ | 非空检查 |
| Algorithm | el-select | ✅ | 选项从 API 动态加载，显示 "名称 (cpu/gpu)" |
| Input Files | el-upload / el-input(textarea) | ✅ | 两种模式切换：上传（drag + multiple，.csv/.txt/.xlsx）或手动路径（每行一个） |
| Downsample | el-input-number | - | min=100, max=50000, step=1000, 默认 5000 |
| Submitter | el-input | - | 可选 |
| Prompt Template | el-select（仅 ChatTS） | - | GPU → ChatTS 时显示，模板从 API 加载 |
| Max Tokens | el-input-number（仅 GPU） | - | min=256, max=8192, step=256, 默认 2048 |
| Submit Now | el-switch | - | 控制立即提交或保存为草稿 |
| 提交按钮 | el-button primary + el-button | - | "Create & Submit" / "Save Draft" + "Cancel" |

**条件显示逻辑**:
- 选择 GPU 算法时 → 显示 `el-divider "GPU Model Options"` + Max Tokens
- 选择 chatts 时 → 额外显示 Prompt Template 选择器

**label-width**: 160px，max-width: 700px

#### ModelCenter.vue 注册/编辑模型对话框

| 字段 | 组件 | 必填 | 验证/约束 |
|------|------|------|-----------|
| Name | el-input | ✅ | placeholder: "e.g. ChatTS-8B" |
| Family | el-select | ✅ | 固定选项: chatts/qwen/adtk_hbos/ensemble/wavelet/isolation_forest/stl_wavelet |
| Runtime Type | el-select | ✅ | 固定选项: transformers/transformers+lora/sklearn/scipy/vllm |
| Version | el-input | ✅ | placeholder: "e.g. v1.0" |
| Artifact URI | el-input | - | 模型文件路径 |
| Description | el-input textarea rows=3 | - | 描述信息 |

**对话框**: el-dialog width=560px，label-width: 120px

### 数据获取策略

#### 轮询策略

| 页面 | 间隔 | 条件 | API |
|------|------|------|-----|
| TaskList | 5 秒 | 始终轮询（onMounted 启动） | taskApi.list() + taskApi.stats() 并行 |
| TaskDetail | 3 秒 | 仅 isRunning 状态（pending/queued/running） | taskApi.get() |

#### 懒加载策略

| 场景 | 触发 | 说明 |
|------|------|------|
| Results Tab | tab-change 到 "results" | 首次切换时 fetchResults()，之后缓存 |
| Logs Tab | tab-change 到 "logs" | 首次切换时 fetchLogs()，之后缓存 |
| D3 图表 | 点击 "View Chart" 按钮 | 按 resultId 单独加载，chartDataMap 缓存 |

#### 初始化加载

| 页面 | onMounted 行为 |
|------|----------------|
| TaskList | fetchTasks()（含 list + stats 并行） |
| TaskCreate | configApi.algorithms() + configApi.chattsPrompts() 并行，失败有降级默认值 |
| TaskDetail | fetchTask()（仅 info） |
| ModelCenter | fetchModels() + fetchGpuStatus() 并行 |

### 交互模式

#### 操作按钮可见性（基于状态）

```
任务看板 / 任务详情:
  Submit  → 仅 draft 状态
  Cancel  → pending / queued / running 状态（cancellableStatuses）
  Retry   → failed / timeout 状态（retryableStatuses）
  Detail  → 始终可见（仅看板）
  Back    → 始终可见（仅详情）

模型中心:
  Edit     → 始终可见
  Activate → status ≠ active 时可见
  Archive  → status = active 时可见（带 ElMessageBox.confirm 确认）
```

#### Loading 状态

| 场景 | 实现方式 |
|------|----------|
| 表格数据加载 | `v-loading="loading"` 在 el-table 上 |
| 提交按钮 | `:loading="submitting"` 在 el-button 上 |
| 图表加载 | `:loading="chartLoading[row.id]"` 按 resultId 独立控制 |
| GPU 状态卡片 | `v-loading="gpuLoading"` 在 el-card 上 |
| 详情页整体 | `v-loading="loading"` 在根 div 上 |

#### 消息反馈

| 场景 | 方式 |
|------|------|
| 操作成功 | ElMessage.success() |
| 操作失败 | ElMessage.error(e.response?.data?.detail) |
| 表单校验不通过 | ElMessage.warning() |
| 确认框 | ElMessageBox.confirm()（归档模型时） |

#### 导航跳转

| 触发 | 目标 |
|------|------|
| 点击任务名（el-link） | `/tasks/${row.id}` |
| 点击 New Task 按钮 | `/tasks/create` |
| 创建任务成功 | `/tasks/${taskId}` |
| 点击 Back 按钮 | `router.push('/tasks')` |
| 点击 Cancel（表单） | `router.back()` |
| 侧边栏菜单 | el-menu router 模式自动跳转 |

### D3.js 时序图组件规格（TimeSeriesChart.vue）

#### 输入 Props

| Prop | 类型 | 数据结构 |
|------|------|----------|
| series | Array (required) | `[{time, value, index, outlier, global_mask}]` |
| segments | Array | `[{start, end, score, length}]` |
| pointName | String | 测点名（Y 轴标签） |
| method | String | 算法名 |

#### 布局尺寸

```
总高度: 460px
主图 margin: { top: 20, right: 60, bottom: 100, left: 70 }
缩略图 margin: { top: 0, right: 60, bottom: 20, left: 70 }
缩略图高度: 60px
主图-缩略图间距: 30px
SVG 宽度: 容器宽度（响应式 ResizeObserver）
```

#### 视觉元素

| 元素 | 颜色/样式 | 说明 |
|------|-----------|------|
| 时序线（主图） | #409EFF, stroke-width: 1.2 | d3.line()，支持 null 断线 |
| 时序线（缩略图） | #409EFF, stroke-width: 0.8 | 降采样（每 1000 点取 1 点） |
| 异常区域高亮 | #F8766D, opacity: 0.15 + score * 0.05 | 矩形覆盖，透明度随分数增强 |
| 离群点 | #F8766D, r=2.5 圆点 | outlier=1 的数据点 |
| Tooltip 指示线 | #999, stroke-dasharray: 3,3 | 虚线垂直线 |
| Tooltip 圆点 | #409EFF, r=4, 白色描边 | 跟随鼠标的数据点 |
| Tooltip 面板 | 白底 0.95 透明度, 阴影, 圆角 4px | 显示时间 + 数值 + 离群标记 |

#### 交互功能

| 功能 | 实现 |
|------|------|
| 缩放 | d3.brushX() 在缩略图上拖拽选区 → 主图联动缩放 |
| 悬停 | mousemove → 最近点查找 → 显示 Tooltip（时间/值/离群） |
| 响应式 | ResizeObserver 监听容器宽度变化 → 重绘 |
| 数据更新 | watch(series, segments) → nextTick(render) 自动重绘 |

#### 坐标轴

| 轴 | 配置 |
|----|------|
| X 轴（主图） | 8 ticks，旋转 -30°，显示日期部分 |
| Y 轴（主图） | 6 ticks，左侧 |
| Y 轴标签 | 旋转 -90°，显示 pointName |
| X 轴（缩略图） | 6 ticks，字号 10px |

### API 客户端结构（api/index.js）

```
axios.create({ baseURL: '/api/v1', timeout: 30000 })

导出模块:
├── taskApi     → /inference/tasks（list/get/create/update/submit/cancel/retry/stats/results/logs/chartData）
├── modelApi    → /models（list/get/create/update/activate/archive）
├── uploadApi   → /uploads（upload, timeout: 120000, multipart/form-data）
├── configApi   → /inference/configs（algorithms/chattsPrompts）
└── gpuApi      → /models/vllm/status
```

### 共享常量（utils/constants.js）

```javascript
statusColors    // 8 种任务状态 → el-tag type 映射
statusLabels    // 8 种任务状态 → 中文/英文显示标签
cancellableStatuses  // ['pending', 'queued', 'running']
retryableStatuses    // ['failed', 'timeout']
gpuAlgorithms        // ['chatts', 'qwen']
```

### 可复用设计模式

| 模式 | 使用位置 | 说明 |
|------|----------|------|
| 统计卡片行 | TaskList | el-row + el-col(:span=3) + el-card(shadow="hover")，点击可筛选 |
| 操作栏 | TaskList, ModelCenter | flex 布局 justify-content: space-between，左侧筛选/右侧创建按钮 |
| 状态标签 | 全局 | el-tag :type="statusColors[status]"，统一颜色映射 |
| CRUD 对话框 | ModelCenter | el-dialog + el-form，编辑/创建复用同一表单 |
| Tab 懒加载 | TaskDetail | el-tabs @tab-change 触发首次数据获取 |
| 轮询模式 | TaskList, TaskDetail | onMounted 启动 setInterval，onUnmounted 清理 |
| 错误降级 | TaskCreate | API 加载失败时使用本地默认值 |
| 确认对话框 | ModelCenter (Archive) | ElMessageBox.confirm() 保护危险操作 |
| 分页 | TaskList | el-pagination + offset/limit 计算 |
| 固定右列 | TaskList, ModelCenter | 操作列 fixed="right" 保证可见 |

## 任务状态机

```
draft → pending → queued → running → completed
                                   → failed    → pending (retry)
                                   → cancelled
                                   → timeout   → pending (retry)

可取消状态：draft, pending, queued, running
可重试状态：failed, timeout
```

## 执行调度链

```
API submit → Celery task dispatch
  ├── CPU 方法 (ensemble/wavelet/iforest/adtk_hbos/stl_wavelet)
  │   → _run_direct() → dispatcher.run() → 算法模块直接调用 → lb_eval 打分
  │
  ├── GPU 方法 (chatts/qwen)
  │   → _run_vllm() → dispatcher.run_vllm() → HTTP POST 到 GPU 服务
  │     ├── chatts_serve.py (:8001) → transformers 推理 (chatts conda env)
  │     └── vLLM serve     (:8002) → vLLM 0.11.0 推理 (qwen-vllm011-clean conda env)
  │   → 解析模型输出 → lb_eval 打分
  │
  └── 回退 (其他方法)
      → _run_subprocess() → CLIExecutorAdapter → subprocess 调旧 run.py

所有路径 → 写结果文件 (CSV + metrics.json + segments.json)
         → 更新 DB (status + result_summary + InferenceResultIndex)
         → 写执行日志 (execution.log)
```

## 部署拓扑

```
┌──────────────────────────────────────────────────────────────┐
│  Platform Backend (:8100)        systemd: inference-backend  │
│  FastAPI + Celery Worker         systemd: inference-celery   │
│  conda env: inference-platform (Python 3.11)                 │
└──────────┬───────────────────┬───────────────────────────────┘
           │                   │
  ┌────────▼──────────┐  ┌────▼────────────────┐
  │ chatts_serve.py   │  │ vLLM 0.11.0 serve   │
  │ :8001 (GPU 0)     │  │ :8002 (GPU 1)       │
  │ ChatTS-8B         │  │ Qwen3-VL-8B         │
  │ 15.4GB fp16       │  │ ~18.1GB half        │
  │ transformers      │  │ run_qwen_vllm.sh    │
  │ OpenAI API 兼容    │  │ OpenAI API 兼容      │
  │ systemd: chatts   │  │ systemd: qwen-serve │
  │   -serve          │  │                     │
  └───────────────────┘  └─────────────────────┘
  conda: chatts            conda: qwen-vllm011-clean

服务管理: scripts/platform.sh (start/stop/restart/status/logs)
全部 4 服务: Restart=always, systemctl --user enable
硬件：2× NVIDIA RTX 2080 Ti (22GB)
```
