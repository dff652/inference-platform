# 开发进度与待办

## 最后更新：2026-03-15

## 一、一期进度总览

### 已完成

- [x] 设计文档评审（7 份文档，结论可行）
- [x] 项目脚手架搭建（34 个文件）
- [x] 5 个核心数据模型（InferenceTask / ModelEntity / ConfigTemplate / DataSource / ResultIndex）
- [x] 任务状态机（8 状态 + 转换校验 + cancel/retry 支持）
- [x] 20+ API 接口（任务/模型/配置/数据源 四组 CRUD）
- [x] 执行适配器（CLIExecutorAdapter，subprocess 调旧 run.py）
- [x] Celery 任务定义（代码就绪）
- [x] conda 环境（inference-platform, Python 3.11）
- [x] 端到端联调（adtk_hbos + PI_20412.PV.csv 成功）
- [x] Docker 编排（backend + celery_worker + redis）
- [x] Git 仓库初始化

### 待完成

#### P0 - 阻塞上线（已全部完成 2026-03-15）

- [x] 启动 Redis，验证 submit API → Celery → 执行适配器全链路
- [x] 补充 `GET /tasks/{id}/logs` 接口（读取 execution.log）
- [x] 补充 `GET /tasks/{id}/results` 接口（读取 metrics + segments JSON）
- [x] 适配器结果收集增强：解析 metrics.json 回写 result_summary
- [x] InferenceResultIndex 写入逻辑（从 metrics.json 提取 score_avg / segment_count 等）
- [x] 修复：状态机 PENDING→QUEUED→RUNNING 正确转换
- [x] 修复：submit 先 commit 再 dispatch Celery（消除竞态条件）
- [x] 修复：Celery task 在 session 内读取 snapshots（消除脱离 session 问题）

#### P1 - 重要

- [ ] Vue 3 前端脚手架搭建
- [ ] 推理任务创建页面
- [ ] 推理监控页面（任务列表 + 状态筛选 + 详情）
- [ ] 模型中心页面
- [ ] 注册模型种子数据（ChatTS-8B / Qwen-3-VL / ADTK-HBOS / Ensemble）
- [ ] 注册配置模板种子数据

#### P2 - 可延后

- [ ] 数据源连通性测试（IoTDB / 文件系统实际检测）
- [ ] 数据源管理页面
- [ ] 配置管理页面
- [ ] ChatTS / Qwen 方法的执行适配器验证（需 GPU）
- [ ] 任务超时自动处理
- [ ] 批量任务支持（多文件 → 多子任务）

## 二、已知技术风险

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| SQLite 并发写入限制 | Celery worker 并发写 DB 可能冲突 | 一期单 worker；二期切 PostgreSQL |
| ChatTS/Qwen 环境未验证 | 可能需要不同 conda 环境 | 适配器层可通过配置切换 Python 路径 |
| 旧项目 run.py 输出格式不稳定 | 不同方法的 JSON 结构可能有差异 | 结果解析需要做容错处理 |

## 三、里程碑

| 里程碑 | 目标日期 | 内容 |
|--------|----------|------|
| M1: 后端 API 可用 | ✅ 2026-03-15 | API + 适配器 + 端到端联调 |
| M2: Celery 全链路 | ✅ 2026-03-15 | Redis + submit + 异步执行 + 结果回收 |
| M3: 前端 P0 页面 | 待定 | 任务创建 + 监控 + 模型中心 |
| M4: 一期灰度上线 | 待定 | 内部小范围使用 |
